{
  "name": "arazzo-proof-runner-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite site --host 0.0.0.0",
    "build": "npm run build:site",
    "build:site": "vite build --config site/vite.config.js",
    "test": "cargo test && npm run build:site && npm run test:site",
    "test:site": "node --test site/tests/*.test.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "playwright": "1.58.2",
    "vite": "7.3.6"
  }
}
